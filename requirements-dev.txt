# Runtime dependencies
-r app/requirements.txt

# Test tooling (subset also installed in the Docker image via requirements-test.txt)
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-timeout>=2.4.0
hypothesis>=6.165.10
websocket-client>=1.9.0

# JS card testing (development only — never installed in the Docker image;
# no guaranteed prebuilt wheel on ARM64, and building the C extension isn't
# worth requiring on an ODROID-M1 just for the on-device test button).
# Lets test_card.py execute the real nibe-entity-manager-card.js instead of
# a hand-written Python mirror of its logic. Tests skip cleanly if absent.
quickjs>=1.19.4

# Static analysis (development only — never installed in the Docker image)
ruff>=0.16.4
mypy>=2.3.1
types-PyYAML>=6.0.12.20260815
vulture>=2.16
bandit>=1.9.4
pip-audit>=2.10.1
mutmut>=3.7.0
