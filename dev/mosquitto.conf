# Disposable broker config for dev/mosquitto.sh and the MQTT integration
# suite. Anonymous access and no persistence are fine here — this container
# is never reachable from outside 127.0.0.1 and is torn down between runs.
listener 1894
allow_anonymous true
persistence false
