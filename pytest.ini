[pytest]
testpaths = tests
pythonpath = app
# Per-test timeout. Stateful Hypothesis tests (EntityManagerStatefulTest etc.)
# run 500 examples with stateful_step_count=50 and take ~3 min on ODROID-M1.
timeout = 300
